FROM python:3.10-slim

WORKDIR /app

COPY mission-control/api/requirements.txt .

RUN pip install --no-cache-dir -r requirements.txt

COPY mission-control/api /app/api

CMD ["uvicorn", "api.main:app", "--host", "0.0.0.0", "--port", "8000"]